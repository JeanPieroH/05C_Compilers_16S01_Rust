fn main() {
    let f1: f64 = 10.0;
    let f2: f64 = 20.5;
    let is_gt: bool = f1 > f2;
    let is_lt: bool = f1 < f2;
    let is_ge: bool = f1 >= 10.0;
    let is_le: bool = f2 <= 20.5;
    let is_eq: bool = f1 == f2;
    let is_neq: bool = f1 != 10.0;
}
