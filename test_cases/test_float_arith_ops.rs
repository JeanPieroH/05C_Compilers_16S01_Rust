fn main() {
    let x: f64 = 10.5;
    let y: f64 = 2.5;
    let sum_val: f64 = x + y;
    let diff_val: f64 = x - y;
    let prod_val: f64 = x * y;
    let quot_val: f64 = x / y;
    let neg_val: f64 = -x;
}
